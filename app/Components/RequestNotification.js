import { StyleSheet, Text, View } from "react-native";
import React from "react";

const RequestNotification = () => {
  return (
    <View>
      <Text>RequestNotification</Text>
    </View>
  );
};

export default RequestNotification;

const styles = StyleSheet.create({});
